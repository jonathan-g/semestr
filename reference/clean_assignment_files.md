# Clean up assignment riles.

Clean up assignment files to prepare for freshly regenerating
assignments.

## Usage

``` r
clean_assignment_files(verbose = TRUE, dry_run = TRUE)
```

## Arguments

- verbose:

  Send progress messages to the console

- dry_run:

  Simulate the cleaning without actually deleting files.
